let min = 200
let current = 50
let max = 100
if ((current >= min) && (current <= max)){
    console.log ("Very good !")
} else if ((min > max)){
    console.log ("Error: 'min' can't be greater than 'max', try again !")
}